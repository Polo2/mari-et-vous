<%= simple_form_for([@wedding, @task, @element]) do |f| %>

  <%= f.input :content, as: :text %>
  <%= f.input :public, collection: [public_for(false), public_for(true)], as: :radio_buttons %>
  <%= f.button :submit %>

<% end %>


